# SUI Network Configuration
NEXT_PUBLIC_SUI_NETWORK=testnet
NEXT_PUBLIC_SUI_RPC_URL=https://fullnode.testnet.sui.io

# DocuLock Contract Addresses (update after deployment)
NEXT_PUBLIC_DOCULOCK_PACKAGE_ID=

# Note: NEXT_PUBLIC_DOCULOCK_REGISTRY_ID is NOT set here for the demo.
# The registry is created via the UI after connecting your wallet.
# Once created, the registry ID is stored in localStorage automatically.
# For production, you would set this value after deploying the contract.
